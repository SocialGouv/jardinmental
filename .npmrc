# Configuration pnpm
# Hoisting pour compatibilité avec certains packages React Native
shamefully-hoist=true

# Forcer React et React-Native à être hoisted pour éviter les duplications
public-hoist-pattern[]=*react*
public-hoist-pattern[]=*expo*

# Configuration pour les build scripts
ignore-scripts=true
